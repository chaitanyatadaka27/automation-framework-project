base.url=http://localhost:9000

ui.url=http://localhost:9000

# Credentials
username=admin
password=admin
invalid.username=wrong
invalid.password=wrong

# Browser
browser=chrome

# Retry
retry.count=2